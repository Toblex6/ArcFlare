# AGENTS.md Archive — Dated Batch Findings

Moved out of root AGENTS.md on 2026-09-02 to keep the root file lean (Claude Code's 40k-char root-file limit). This is the full historical log of audit/build batches, verbatim, unedited. Not auto-loaded by Claude Code — read on demand.

!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

<!-- END:nextjs-agent-rules -->

## Known findings (tracked)

- **Arc Testnet USDC fees — re-measured 2026-08-18; the old "~0.001553 flat / ~12.3% into contracts" figures do NOT reproduce.** Fresh measurements (swap-pool-e2e): EOA→EOA flat **0.001028** (exact at 0.05/0.50/1.00/5.00); EOA→contract **+0.00217–0.0044** (~0.2%); contract→EOA recipient short **0.0014** (payroll cancel-refund) to **0.0022** (swap pool outbound); EURC **fee-free everywhere measured**. The swap pool's bookkeeping tracks exact amounts, so each USDC-outbound op leaks ~0.0022 from pool reserves unseen by LP math. The payroll contract is NOT reliably fee-exempt as sender. Re-verify on mainnet before trusting escrow math.
- **Fee = per-target, not per-op-type — re-confirmed 2026-08-18 (repro-balance-views 14/14)**: ERC-20-interface transfer fee varies by DESTINATION even EOA→EOA (0.001028 to `0x1111…`, 0.001712 to fresh EOAs; EOA→swap-pool ~0.2% of value; EOA→payroll ~12% of value). **Native value-sends are FEE-FREE (cost = amount + gas ~0.000486)** — use `sendTransaction`/native `value` for EOA→EOA transfers (agent payments do this). ERC-20 `transfer` is only needed for `approve`/`transferFrom` flows (pool, payroll funding).
- **Native USDC vs ERC-20 view are ONE asset, two decimal views — NOT a bug**: `getBalance()` is 18-dec, `balanceOf(0x3600000000000000000000000000000000000000)` is 6-dec; same-block reads agree within <1e-6 USDC (the ERC-20 view truncates sub-1e-6 dust). The ~0.31 "divergence" reported 2026-08-18 was a measurement artifact (prints compared at different blocks across a +1.0 credit, a 1.5531 probe spend, approvals and gas; one debug script also printed `getBalance()/1e6` instead of `/1e18`). Always compare with a FIXED blockTag on both views. Native sends emit EIP-7708 system `Transfer` logs (18-dec, emitter `0xffff…fffe`); ERC-20 transfers emit both the system log and the contract's own 6-dec log.
- **Payroll x402 wiring RESOLVED (2026-08-18)**: live `fundPayrollViaX402()` in `src/lib/payroll/payrollExecution.ts` (dead-code version in `stubs/dead-code/` is superseded — do not re-introduce it). E2E `scripts/payroll-x402-e2e.ts` 15/15: pre-flight 403, settle, seller sweep (with 120s `totalBalance` poll), on-chain `checkAndRecordSpend`, `recordSpend`, `fundBatchFor` (self-healing allowance + refund-on-failure), `executeBatch`.
- **Seller wallet mismatch (important)**: `SELLER_WALLET_ADDRESS` (`0xa8d1d913…`) has NO private key anywhere — **~0.554 USDC is stranded** in the x402 batching contract depositor balance for that address (old marketplace settlements + E2E settles). **CLOSED 2026-08-18**: accepted as unrecoverable testnet loss (no key exists, testnet only) — no further action, do not pay to `0xa8d1d913` again. Code uses `SELLER_ADDRESS` (`0xc119bb61…`) + `SELLER_PRIVATE_KEY`.
- **Circle `/settle` executes asynchronously** — API debits instantly, on-chain credit took 40+ min (settles #2/#3). The sweep polls up to 120s; a slower settlement still lands later (funds are safe in the seller gateway depositor) but the request 500s and a PENDING_REVIEW row is written. Check `totalBalance(usdc, SELLER_ADDRESS)` on batching contract `0x0077777d7EBA4688BDeF3E311b846F25870A19B9` before assuming failure.
- **Stale RPC fixed**: `.env.local` `ARC_TESTNET_RPC` was `rpc-testnet.arc.xyz` (dead endpoint) → now `https://rpc.testnet.arc.network`. **2026-08-18 update**: `rpc.testnet.arc.network` developed intermittent node-side TLS failures (ECONNRESET/bad record MAC on every retry while curl stayed healthy); `rpc.quicknode.testnet.arc.io` is node-healthy but free-tier rate-limits (`-32011`) under suite bursts — kept **`https://rpc.testnet.arc.network`** as primary in both env files, and `scripts/payroll-e2e.mjs` now wraps the provider with a transport-level retry/backoff (ECONNRESET/bad record MAC/-32011/TIMEOUT) that absorbs the flakes. Docs list alternates: `rpc.testnet.arc.io` (primary, slow), `rpc.blockdaemon.testnet.arc.io`, `rpc.drpc.testnet.arc.io`.
- **Explorer API caps `tokentx` at 24 rows** (offset pages 2+ are empty) — feed-based reconciles miss older transfers; use native tx lists instead.
- **Arc explorer does not support contract verification** — deployed contracts (`ArcFlarePayroll`, `ArcFlareSpendLimit`, `ArcFlareSwapPool`) are unverified; flatten source before shipping.
- **ArcFlareSwapPool deployed** `0xaD4F3634a64685CB7dff08B82fb742e4ca7f7451` (tx `0x5aebb350…`), `SWAP_POOL_CONTRACT_ADDRESS` set in `.env` + `.env.local`. Seeded USDC/EURC ≈ 1.08 ratio (reserves ~22.5/18.6 after tests). EURC = `0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a`, 6 decimals (verified on-chain). EURC testnet is only obtainable via faucet.circle.com (captcha-gated; relayer EOA holds the drip).
- **Agent payments live (2026-08-18, E2E 20/20)**: `POST /api/agents/[id]/pay` = agent-to-agent payment — direct on-chain NATIVE value-send from the agent's per-agent x402 payment EOA (`X402EoaWallet.agentRegistryId`, AES-GCM at rest; auto-provisioned via `GET /api/agents/[id]/wallet`), NOT x402/Gateway (boundary doc: A2A = Jobs-pattern direct settlement). Spend policy = `ArcFlareSpendLimit.setLimit` via `POST /api/agents/[id]/policy` (owner-gated contract + merchant-control route check). Enforcement order: pre-flight `checkSpendAllowed` → `checkAndRecordSpend` BEFORE the transfer (irreversible A2A — cap can never be exceeded; a revert means no payment) → native send → recipient credit delta verified at the receipt block → PaymentLog with real `arcTxHash`. Over-cap → 403, no funds move. Fee-free by construction (native sends measured = amount + gas only).
- **Audit-fix batch 2026-08-19 (6 findings fixed + regression)**: C1 unconditional payer-control guard in settle/route.ts (internal keys may only settle rows whose payer == `AGENT_OWNER_WALLET_ADDRESS`; merchant own-rows with non-address payers preserved; expired rows now 403 for internal, 400 EXPIRED for merchant keys) + M5 `serviceKey.active` check; C2 initialize agentSCA ownership gate; C3 brain `agent_pay_agent` payer pinned to `AGENT_OWNER_WALLET_ADDRESS`; H5 POST `/api/payments/stream` deleted (405; GET kept; `/create` unaffected; dup `src/app/checkout/pay/route.ts` deleted — only `/api/checkout/pay` remains, which now 403s for every non-platform-agent payer); H1 seller-gateway withdraw restricted to `SELLER_GATEWAY_TREASURY_ADDRESSES` allowlist (or seller's own EOA when unset; single-entry allowlist = default recipient); H9 `crypto.randomInt` + `Merchant.resetCodeAttempts` (burn at ≥5; same 400 either way, no oracle). E2E: checkout-e2e 52/52, checkout-hardening 74/74, payroll-e2e 20/20, agent-pay 20/20, payroll-x402 15/15, spendlimit 13/13, smoke 5/5, swap-pool 9/9, repro-balance-views 14/14, tsc clean, build 110/110. Migration `0005_reset_code_attempts` applied.
- **Audit-fix batch 3, 2026-08-19 (Opus 5 follow-up — scheduled default-wallet drain closed, 5/5)**: (1) `/api/payments/scheduled/run` NO LONGER falls back to `DEFAULT_PAYER_WALLET_ID` — `executeOnePayment` throws when `scheduled.payerWalletId` is null ("no resolved payer wallet… refusing to execute against a shared default"); the fallback constant was removed from run/route.ts. Creation (`scheduled/route.ts`) now RESOLVES the payer's bound wallet at persist-time (consumer → `ConsumerAccount.circleWalletId`; merchant's own wallet → `Merchant.circleWalletId`; platform agent → `DEFAULT_PAYER_WALLET_ID` — explicit resolution, same convention as settle Path B, NOT a fallback; registered agent → `AgentRegistry.circleWalletId`) and refuses to persist (400 "refusing to persist a recurring payment that cannot pay") when unbound. Body-supplied `payerWalletId` is ignored (was a free-choice debit vector). (2) brain `complete_or_reject_job`: `evaluatorSCA` REMOVED from the tool schema (jobId/verdict/reason; required jobId+verdict) and the executor pins `evaluatorSCA: process.env.AGENT_OWNER_WALLET_ADDRESS` (create_agent_job keeps LLM-named evaluator — that's a job-contract field, not the signer). (3) `docs/settlement-architecture.md` stale `/api/checkout/pay` + "fallback DEFAULT" wording fixed (fail-closed, explicit platform-agent resolution). (4) DELETED dead `src/lib/auth/verifyCallerControlsAddress.ts` + its only importer `src/lib/tokens/swapExecution.ts` (grep-proven unreachable). (5) audit-repro gaps fixed: static schema regex now matches the codebase's real UNQUOTED schema props anchored on `{` object values (the old quoted-only regex could never fire), evaluatorSCA scoped to complete_or_reject_job (both schema + executor), positive-control asserts payerWalletId explicitly resolved (== settle's DEFAULT_PAYER_WALLET_ID, cross-checked by grepping settle source), and a NEW Exploit 3: a poisoned null-payerWalletId row is REFUSED by live /scheduled/run (receiver on-chain balance provably unchanged via eth_call balanceOf) + creation 400s a merchant payer without a bound Circle wallet. E2E: audit-repro 47/47, checkout-e2e 57/57, checkout-hardening 75/75, payroll-e2e 20/20, agent-pay 20/20, payroll-x402 15/15, spendlimit 13/13, smoke 5/5, swap-pool 9/9, repro-balance-views 14/14, tsc clean, build 109/109. Funding ops this run: relayer/buyer `0x0d9Dc173` +6.0 and seller EOA `0xc119bb61` +2.0 from x402 pool wallet `0x305CBD57` (was 17.99, now ~11.99); seller depositor on batching contract gained 0.12 (slow Circle settle); spendlimit suite consumed the 6.0 top-up (0.1366 was left of it at the first dry-out — native gas is what drains, each op ≈ 0.001-0.16 depending on gas price). Legacy DB rows with null payerWalletId now correctly FAIL at every run tick (10 such rows observed failing — the fix is live on old data).**
- **2026-08-19 RPC instability (recurring)**: the Arc cluster is intermittently flaky on ALL endpoints (TLS bad-record-MAC, ECONNRESET, timeouts; curl stays healthy) and nodes are ~16 blocks OUT OF SYNC with each other (balance/estimate reads can disagree across calls — balance-sensitive tests can revert on stale execution nodes). The spendlimit suite (`payroll-spendlimit-e2e.ts`) and repro-balance-views take the RPC as argv[2]; swap-pool-e2e reads `ARC_TESTNET_RPC`; payroll-e2e has its own retry wrapper. During today's regression `rpc.blockdaemon.testnet.arc.io` was the most reliable; `rpc.drpc.testnet.arc.io` timed out. Retry suites when they flake — they advance further on each run.
- **2026-08-19 testnet funding ops**: the platform default-payer Circle wallet (`0x7a8214…`) ran dry mid-regression (settle 500 `INSUFFICIENT_TOKEN`) — refilled via fee-free native value-send from the seller EOA. Top-up pool for relayer/seller/default-payer: the merchant x402 buyer wallets (`X402EoaWallet` rows, AES-GCM keys decryptable with `X402_WALLET_ENCRYPTION_KEY`) hold 10–18 USDC each and are the only reliable testnet source left; a native `value` send moves the 18-dec USDC asset fee-free. `payroll-spendlimit-e2e` consumes ~0.166 USDC (gas+value) per full run from the relayer. Fresh-EOA ERC-20 transfer fee re-measured **0.001553** today (2026-08-18 recorded 0.001712 — per-target fee varies; not a regression).
- **Audit-fix batch 4, 2026-08-19 (Opus 5 follow-up 2 — nano assignment-default drain closed, the C1-class variant in `nano/settle`)**: `settleOnchain` used `let payerWalletId = DEFAULT_PAYER_WALLET_ID` with a conditional override — the assignment-default shape, invisible to the `|| DEFAULT` regex — so ANY caller controlling either party could POST `/api/payments/nano` with agentSCA = the platform default payer (`0x7a8214…`) and force-settle to drain the shared platform wallet. **PoC-before ran live against the vulnerable code: create 200 → force-settle 200 → real on-chain tx `0x6ccb9231…` settled 0.01 USDC from `0x7a8214da` to the attacker (Transfer logs in the receipt); a second harness run settled ~0.0002 of leftover rows via the internal positive control before the route was restored.** Fix (matches scheduled/route.ts shape): (1) `nano/settle` guard requires PAYER-side control only — `merchantOwnsIt` branch deleted; merchant keys must control agentSCA (their own agent/EOA), error "You do not control the payer (agentSCA) of this settlement."; (2) `settleOnchain` resolves payerWalletId with NO default initializer — platform default reachable ONLY via `isInternalServiceCall` (throw "refusing to debit the shared platform default wallet"), all other payers resolve via AgentRegistry with `mode: "insensitive"` lookup and fail closed if unregistered; (3) identity comparisons case-insensitive (`agentSCANormalized === DEFAULT_PAYER_SCA.toLowerCase()`) — mixed-case `0x7A8214…` variants no longer bypass (verified 403); (4) `nano/route.ts` create guard likewise payer-side (`controlsMerchant` gone; platform default only via internal key); (5) `/nano` UI no longer prefills agentSCA with the default payer (`useState("")`). audit-repro extended: assignment-default shape regex (`let payerWalletId = DEFAULT` initializer must not exist), nano/settle + nano/route + nano/page.tsx added to staticProofs, NEW Exploit 4 (merchant create → 403; poisoned pre-existing row force-settled → 403, row untouched, receiver on-chain balance unchanged; mixed-case → 403; internal key still passes guard and resolves the default wallet, fails closed on empty batch). E2E: audit-repro 67/67, http-auth-tests 17/17, checkout-e2e 57/57, checkout-hardening 75/75, payroll-e2e 20/20, agent-pay 20/20, payroll-x402 15/15, spendlimit 13/13, smoke 5/5, swap-pool 9/9, repro-balance-views 14/14, tsc clean, build 109/109. Funding ops this run: buyer/relayer `0x0d9Dc173` +7.5 and seller EOA `0xc119bb61` +4.0 from x402 pool `0x305CBD57` (now ~9.5); buyer drained to 0.64 by agent-pay (1.0/send) + swap-pool probes (1.553) before the +3.0 top-up. One harness-side observation: the internal-key positive control in Exploit 4 will settle REAL leftover rows for the default-payer pair if any exist (it did once, ~0.0002 USDC) — delete the poisoned row BEFORE the internal call (harness now does).**
- **Audit-fix batch 2, 2026-08-19 (ownership model + brain pins; exploits reproduced, 27/27)**: (1) `src/lib/wallet/verifyCallerControlsAddress.ts` — the ApiKey branch now lets the internal service key act ONLY as the platform agent (`AGENT_OWNER_WALLET_ADDRESS`); merchants keep the ownedAgents branch (merchant keys live on the `Merchant.apiKey` column, so the ApiKey table = service keys only). An API key grants NO control of an arbitrary AgentRegistry address. (2) settle/route.ts — the C1 merchant escape hatch is fully removed: a merchant can no longer settle its own `pending@checkout` link row (only real-0x payers they control: own wallet or own agent); the guard now runs BEFORE the atomic lock (rejected calls leave the row PENDING — no more PROCESSING_ONCHAIN parking/denial-of-settlement); Path B resolves the signing wallet from ConsumerAccount/AgentRegistry only and fails closed (refuses DEFAULT_PAYER_WALLET_ID for any payer except the platform agent). (3) `/api/checkout/pay` deleted (was already 403-only; zero callers — CheckoutWidget settles via verify-onchain). (4) brain LLM-controlled payer/wallet fields removed from AGENT_TOOLS schemas + executors pinned to env: `clientSCA/clientWalletId` (create_agent_job), `payerSCA/payerWalletId` (run_agent_payroll), `payerSCA` (setup_agent_subscription), `senderSCA/senderWalletId` (route_cross_chain), `evaluatorWalletId` (complete_or_reject_job), `validatorWalletAddress` (record_agent_reputation). Also: initialize/route.ts now exempts `AGENT_OWNER_WALLET_ADDRESS` from the AgentRegistry lookup for INTERNAL callers (fixes a 404 regression: `agent_pay_agent` pins payer to the env address which has no registry row — the internal key IS its credential); `.env` AGENT_OWNER_*/AGENT_VALIDATOR_*/AGENT_TOKEN_ID reconciled to `.env.local` (851225/0x7cf8ee2a…/ca09c22f…) — **verify before any prod deploy that reads `.env`**. E2E: audit-repro 27/27 (fresh merchant → payment-link → merchant-key settle 403 + row untouched; internal-key settle 403; internal-key schedule against another tenant's agent 403 + no row; positive control internal-key platform-agent schedule 200; deleted route 404; static code proofs), checkout-e2e 57/57, checkout-hardening 75/75 (idempotency/expiry now exercised via the internal-key platform-agent flow — the only funded settle path), payroll-e2e 20/20, agent-pay 20/20, payroll-x402 15/15, spendlimit 13/13, smoke 5/5, swap-pool 9/9, repro-balance-views 14/14, tsc clean, build 109/109. Suite notes: swap-pool-e2e burns ~2.56 USDC/run (fee probe 1.554 + swap 1.0) from BUYER_ADDRESS (`0x0d9Dc173…`); the settlement-based suites debit ~0.01/run from `0x7a8214…`; fee probe re-measured 0.001027/0.001028 again today. Top-ups this run: 0x7A780c0b (7.18 left), 0x6d2E82Ef (12.8 left) from the x402 pool.
- **Audit-fix batch 5, 2026-08-19 (Opus 5 follow-up 3 — SSRF, telegram double-withdraw, scheduled/run claims, payroll-run idempotency, spend-limit bootstrap, sweep/fee accounting, rate-limit identity, env validation, session invalidation; 13 findings)**: (H2) `verifyCallerControlsAddress` callers all null-check — audit-repro static proof scans every src/ file (34 calls across 11 files, guards >= calls per file; helper file excluded) + NEW Exploit 5 live proof (fresh merchant api-key GET `/agents/[id]/policy` and POST `/agents/[id]/pay` on another tenant's agent -> 403, no funds moved). (H3) **spend-limit bootstrap front-run closed**: every NEW agent payment EOA gets an explicit default limit at provisioning (`ensureAgentDefaultSpendLimit` in `spendLimitEnforcer.ts`, called from `x402-wallet.ts` creation branch only) — relayer signs `setLimit(agent, 100e6, 86400)` so the relayer becomes the limit owner BEFORE any attacker can first-call-wins the contract's unowned slot; `POST /agents/[id]/policy` now 403s when the on-chain limit owner != relayer ("limit ownership taken by another address"); `GET /agents/[id]/policy` is no longer public (requires caller control — the EOA handle was a bootstrap-leak). Contract-level `checkAndRecordSpend` ACL (owner-only) still requires a contract deploy — documented as the remaining piece; backend call sites are only `agentPay.ts`/`payrollExecution.ts` (relayer-signed), proven by static check. (H4) SSRF guard `src/lib/security/ssrfGuard.ts` (https-only; blocked localhost/.local/.internal + v4/v6 CIDRs incl. mapped/NAT64; DNS-rebinding resolve w/ 5s timeout) enforced at marketplace create/PATCH/pay proxy-time — `scripts/ssrf-guard-tests.ts` 30/30. (H6) Telegram double-withdraw: `TelegramWithdrawalIntent.status` (migration `0006_telegram_withdrawal_status`), `handleConfirmWithdraw` atomic PENDING->EXECUTING claim (rollback on failure, delete on success), webhook `update_id` dedupe moved to `src/lib/telegram/webhookDedupe.ts` (bounded 500; route modules may only export verbs — build enforces). **IMPORTANT: Circle's ARC-TESTNET endpoint REJECTS the `idempotencyKey` param on createTransaction/createContractExecutionTransaction ("API parameter invalid", root-caused live 2026-08-19) — `transferUsdc` no longer forwards it; double-withdraw protection rests on the DB claim alone (`telegram-withdraw-idempotency-tests.ts` 9/9 proves it, incl. an EXECUTING refusal + rollback).** (H7) `/api/payments/scheduled/run` per-row atomic claims: candidates include stale PROCESSING (lastRunAt <= now-5min), each row claimed via conditional updateMany — concurrent runners never double-execute (`scheduled-run-claim-tests.ts` 9/9 incl. a real-funds two-runner proof: runCount 1, COMPLETED, no double-pay). (H8) `/api/payroll/run` hardening (migration `0007_payroll_batch_merchant_id`): amount/address regex validation (max 6 decimals, <=200 recipients), idempotencyKey -> deterministic `payroll_idem_<key>` batchRef with replay + P2002-concurrent replay, GET scoped to `findFirst({ batchRef, merchantId })` (foreign merchant -> 404) — `payroll-run-hardening-tests.ts` 22/22. (M9) `POST /agents/[id]/pay` optional `idempotencyKey`: unique `PaymentLog.idempotencyKey` claim BEFORE the spend record (P2002 -> replay winner), replay returns the SAME txHash, never re-spends, spend record charged once — proven in `agent-pay-e2e.ts` 25/25 (M9 block: same-key replay -> same hash + single SUCCESS row + exact-once spend). (H10) `sweepSettledToRelayer` serialized via module promise-chain mutex (concurrent sweeps can't race the shared seller balance); returns before/after `totalBalance` deltas recorded in the batch results. (H11) payroll funding fee is MEASURED, never assumed: relayer ERC-20 balance delta around `fundBatchFor` (balanceOf at receipt block) -> `feeMeasured` stored in PayrollBatch.results + response. (H12) ratelimit: trusted identity = `req.ip` (XFF never trusted as primary — spoofable), bounded memory store (10k entries, prune-expired-then-oldest), `session` tier (10/min) added; applied to `agents/[id]/pay`, `nano/pay/[endpoint]`, `marketplace/pay/[slug]`, and `consumer/session` (M11 wallet-creation quota). (M10) `walletEnvCheck` now also fails closed on: required secrets (X402 key, JWT secrets, INTERNAL key, Telegram creds, Circle creds) + contract addresses (PAYROLL/SPEND_LIMIT/SWAP_POOL/JOB_ESCROW, 0x40) + `SELLER_GATEWAY_TREASURY_ADDRESSES` allowlist format — validated against `.env` (ok). (M18) session invalidation: migration `0008_merchant_session_version` (`Merchant.sessionVersion INT NOT NULL DEFAULT 0`), login JWT carries `sessionVersion`, reset-password increments it, all three merchant middlewares reject tokens with stale/missing claims (a stolen cookie dies at the reset boundary; all pre-migration sessions invalidated). E2E: audit-repro **81/81**, http-auth-tests 17/17, checkout-e2e 57/57, checkout-hardening 75/75, payroll-e2e 20/20, agent-pay **25/25**, payroll-x402 15/15, spendlimit 13/13, smoke 5/5, swap-pool 9/9, repro-balance-views 14/14, telegram-hardening **28/28**, telegram-withdraw-idempotency 9/9, scheduled-run-claim 9/9, payroll-run-hardening 22/22, ssrf-guard 30/30, tsc clean, build clean. NOTE: `scripts/e2e-jobescrow.mjs` FAILS pre-existing — `relayer()` on `JOB_ESCROW_CONTRACT_ADDRESS` (`0xE1c8646a...`) reverts ("execution reverted"), i.e. the env address doesn't host that contract; suite wasn't in prior passing lists either — needs the correct escrow address before it can run, unrelated to batch-5.
- **2026-08-19 testnet funding ops (batch 5)**: relayer `0x0d9Dc173` ran dry mid-agent-pay (0.55 left) -> +3.0, platform default payer `0x7a8214...` ran dry (2.3e-12, drained by checkout suites + audit-repro settles — telegram /confirm transfers silently fail when it's dry) -> +3.0, seller EOA `0xc119bb61` (0.078) -> +2.0, all via fee-free native value-send from x402 pool wallets (`0x6d2E82Ef` now ~8.7, `0x43F20780` ~7.0; `0x7A780c0b` 2.18, `0xb7a21d9d` ~10, `0xc7406aE0` ~10, `0x305CBD57` 0.49, `0xb328c15f` 0.59). New helper: `scripts/top-up-relayer.ts [amount] [recipient]` picks the richest funded pool wallet automatically (decrypts AES-GCM with `X402_WALLET_ENCRYPTION_KEY`). Relayer also spent ~0.5/agent-pay run (funding + gas) and checkout suites debit the default payer ~0.01/run.
- **Stream / Nanopayment batch, 2026-08-21 (criterion-based nanopayments live on Arc Testnet)**: ArcFlareStream.sol implemented and deployed at `0xd8ca3Bbc212F36666145fAa487D45742eA04A52B` (tx `0x13a3879bcd6dd9ad9f97230f1cd25af949116a468b7e4bc67b7749909cad3009`), bytecode verified (keccak match). Replaces the stale per-second streaming model (H5 POST `/api/payments/stream` deleted in batch 5) with criterion-based tranche payments per the job acceptance criteria. Contract: poster-funded, poster-releases, no refund path, deterministic tranche amounts (floor division, remainder in last tranche), ReentrancyGuard + SafeERC20, MAX_TRANCHES=50. On-chain state: StreamOpened/TrancheReleased/StreamClosed events, getStream(), releasedTranches(), trancheAmounts(). App layer: `src/lib/jobs/nanopaymentSplit.ts` uses real ABI, deterministic bigint tranche math, DB state machine (JobNanopaymentStream/JobNanopaymentTranche), crash-recovery via event scan. Routes: `POST /api/jobs/nanopay/open` (client auth, approve+open), `POST /api/jobs/nanopay/release` (client OR evaluator auth, idempotent + P2002 replay + on-chain recovery), `POST /api/jobs/nanopay/close` (client OR evaluator auth, releases remainder), `POST /api/jobs/nanopay/status` (DB + on-chain mirror). Migration 0009 applied (token + criteriaHash on JobNanopaymentStream). E2E: `scripts/nanopayment-stream-e2e.mjs` 39/39 — create job → set-budget → fund → open stream (3 tranches) → release 0/1/2 with balance deltas → idempotent replay → close → unauthorized 401 → out-of-range 400 → status on-chain mirror verified. Total worker gain = 0.03 USDC (budget exact, no fee on this contract). Known limitation: Arc Testnet USDC applies per-transfer fee to recipient (~0.0014-0.0022 for contract→EOA measured on payroll/swap); this stream contract delivered exact amounts in E2E — verify deltas on mainnet. Regression: tsc clean, next build pass, wallet-env 16/16.
- **Agent Marketplace / AgentCard batch, 2026-08-23 (agent-native economy)**: `AgentRegistry` already held `skills`/`pricing`/`metadataURI`/`reputation` but had no machine-readable discovery. Added `GET /api/agents/[id]/card` (public, generates AgentCard from live `AgentRegistry` row — `agentId`/`erc8004TokenId`/`scaAddress`/`wallet`/`endpoints`/`reputation`/`identity`/`hiring` with `createJobEndpoint`/`hireEndpoint`/`validation.registryAddress`/`supportedChains/Tokens`/`schemaVersion 1.0`), `GET /api/agents/discover` (filters by `skill`/`category`/`minReputation`/`price`/`search`/`sortBy`/`status`, in-memory JSON filtering for `skills`/`pricing` to avoid Prisma path dialect issues, paginated), `POST /api/agents/[id]/hire` (creates `Erc8183Job` via `AGENTIC_COMMERCE_CONTRACT.createJob` with `clientWalletId` resolved server-side via `getCircleClient().getWallet` + `verifyCallerControlsAddress` — never trusts body wallet address, `budget`→`budgetBigInt`, `criteria` validated 1..50, `evaluator` defaults to client, `criteriaHash` via `hashCriteria`, `merchantId` from `actor.id`), and marketplace extension `ApiListing.agentRegistryId` (migration `20260821231938_marketplace_agent_listings` + `20260823120000_make_listing_fields_nullable` to allow `pricePerRequest`/`targetUrl` null for agent listings; `POST /api/x402/marketplace` now accepts `listingType:"agent"` with `agentRegistryId` ownership check `merchantId`==`agent.merchantId` and `status==ACTIVE_AGENT_PROVISIONED`; `GET` now supports `?type=agent|service` and enriches agent listings with `cardUrl`/`hireUrl`/`agent` object; `pay/[slug]` rejects agent listings with 400 hire/card hint). Discovery reuses `AgentRegistry` + `reputation`; hiring reuses `Erc8183Job`/`JobEscrow`/`hashCriteria`/`verifyCallerControlsAddress`/`spend-limit` — no new escrow contract, no wallet type, no payment system. Validation→escrow separation preserved: `ERC8004ValidationRegistry` remains independent, `hire` does not auto-gate `releaseToWorker` on validation. E2E `scripts/agent-marketplace-e2e.mjs` 22/23 (1 flake: `discover?skill=testing` when skills empty; `set-budget`/`submit` now pass with correct `providerWalletId` from `agent.circleWalletId`; RPC `bad record mac` flakes on `create` still). Live probe: `GET /api/agents/1/card` returns full `agentCard` ( `agentId`/`wallet.scaAddress`/`hiring.createJobEndpoint`/`validation.registryAddress` ), `GET /api/agents/discover?search=E2E` returns 5+ `E2E Test Agent` rows. Regression: `tsc --noEmit` clean, `next build` passes. Known migration gap: `pricePerRequest`/`targetUrl` nullable migration applied in schema but DB `NOT NULL` until `prisma migrate deploy` succeeds (Neon `P1001` unreachable from shell — dev server reaches DB). Until deploy, agent listings use dummy `"$0.00"`/`"https://agent.internal/hire"` fallback.
- **Validation-Gated Commerce batch, 2026-08-26 (Build 2: validation-gated escrow release live)**: Validation-gated jobs extend the existing `Erc8183Job` escrow without forking `ArcFlareJobEscrow.sol` — authoritative state is ALWAYS on-chain `ERC8004 ValidationRegistry (0x8004Cb1BF31DAf7788923b405b754f57acEB4272)` via `getValidationStatus`, DB `Erc8183JobValidation` is groundwork for Build 3 trust score (stores `jobId<->validatorSCA+requestHash+status` relationship only). Schema: `model Erc8183JobValidation { id, jobId BigInt @unique FK->Erc8183Job.jobId CASCADE, validatorSCA, requestHash @unique, requestTxHash, responseTxHash, status PENDING|REQUESTED|PASSED|FAILED, tag, required, createdAt/updatedAt, indexes validatorSCA/status }` + `Erc8183Job.validation?` relation; migration `20260823073321_add_job_validation_policy` applied (DB table `erc8183_job_validations` exists). Hiring: `POST /api/agents/[id]/hire` now accepts optional `validation: { required, validatorSCA (0x40), tag }`, validates `validator != client && validator != provider` (self-validation 400), verifies caller controls `clientSCA` via `verifyCallerControlsAddress`, creates job via `AGENTIC_COMMERCE_CONTRACT.createJob` then `createJobValidationPolicy(jobId, validatorSCA)` (idempotent). Validation lifecycle: `POST /api/jobs/[jobId]/validation/request` (client OR provider auth via `verifyCallerControlsAddress`, requires `policy.required`, idempotent replay if `requestHash` exists, finds provider `AgentRegistry` by `providerSCA`, generates `requestHash=keccak256("flarehq_job_validation_"+jobId+validator+Date.now())`, `requestURI=ipfs://arcflare-job-...`, calls `validationRequest(validator, agentId, requestURI, requestHash)` via `Circle` with `agent.scaAddress` as signer (provider owner must be caller), records `REQUESTED`); `POST /api/jobs/[jobId]/validation/respond` (validator-only via `verifyCallerControlsAddress(validatorSCA)`, requires `requestHash`, idempotent PASSED/FAILED replay, calls `validatio
- **Telegram Completion + Docs batch, 2026-08-28**: Telegram gaps closed: `sendTelegramMessage(telegramUserId, text)` in `src/lib/telegram/sendTelegramMessage.ts` posts to `api.telegram.org/bot<token>/sendMessage` using `TELEGRAM_BOT_TOKEN`; `POST /api/jobs/complete` now best-effort notifies the provider if `ConsumerAccount.telegramUserId` exists for `providerSCA` (`✅ Job #<id> paid: <amount> USDC. New balance: /balance`, try/catch never fails completion, fires once per job not per ledger entry — `src/app/api/jobs/complete/route.ts:150-164`); `/help` now lists `/retrygas` (`src/lib/telegram/botHandlers.ts:329-340`) and new `/history`/`/earnings` (`handleHistory` 352-376 reads `Erc8183Job` where `providerSCA == wallet` and `COMPLETED`, last 10 + sum of `budget`; human workers have no `AgentRegistry`/`AgentLedgerEntry` so total derives from job budgets directly, not ledger); webhook `src/app/api/telegram/webhook/route.ts:168-172` dispatches `/history`/`/earnings` → `handleHistory`. Docs: `docs/architecture.mdx` Telegram section (table per command + notification + structural gap note that `JobApplication`→`providerSCA` assignment is not yet wired), `docs/settlement-architecture.md` §6 Telegram read model. Investigation finding: `/apply`→`JobApplication` is a dead end — `getRankedApplicants`/`submitApplication` have no `assignWorker` wire, so Telegram applicants never become `providerSCA` except when named at `jobs/create`/`hire` time; `/deliver`+notification only reach Telegram workers who were explicitly set as `providerSCA`. `src/lib/telegram/botHandlers.ts:352` `handleHistory` scoped by `walletAddress` (own jobs only). Tests: `scripts/telegram-completion-tests.ts` (10 checks: help includes retrygas/history, /history scoping, completion fires once, API failure does not roll back). Regression: tsc clean, build pass, telegram-hardening/deliver still pass (withdraw idempotency untouched).nResponse(requestHash, 100|0, "", 0x00..00, tag)` via `Circle` with `validatorSCA` as signer, records `PASSED|FAILED`); `GET /api/jobs/[jobId]/validation` (no auth, returns `policy + onChain + evidence` with `validationRegistryAddress`, handles pending vs PASSED vs FAILED via `getOnChainValidationStatus` with correct pending detection `response==0 && tag=="" && lastUpdate!=0` — initial 0/"" is pending, not failed). Escrow gating: `POST /api/jobs/complete` now calls `isValidationSatisfiedForJob(jobId)` before `createContractTransaction` — normal jobs (`!policy || !required`) pass, gated jobs require `requestHash` exists AND `onChain.passed==true && validator matches` else `409 {code:"VALIDATION_REQUIRED", validationStatus: reason}` and no funds move (verified via balanceOf delta 0). Fixes: `validationRequest` double-control bug removed (only provider owner required), `pending` detection fixed (was `validator==0` only, now `response==0 && tag==""`), `respond` escaped template bug fixed (`0x\${"0"...}` literal -> `0x00...00`). E2E `scripts/validation-gated-e2e.mjs` **31/31**: Path A normal (hire no validation -> set-budget -> fund -> submit -> complete 200 -> payment >0.008 -> status not required), Path B gated PASS (hire with validation -> fund -> submit -> early complete 409 + no payment -> request 200 -> duplicate request idempotent -> wrong validator 403 -> validator PASS 200 -> status PASSED -> complete 200 -> payment >0.008), Path B2 FAIL (hire -> request -> FAIL 200 -> complete 409 + no payment), unauthorized 401, duplicate idempotent, evidence `jobId/validatorSCA/requestHash/onChainStatus/passed`. Regression: `tsc --noEmit` clean, `next build --webpack` 97s compile + 42s typecheck + collect page data OK (routes include `/api/jobs/[jobId]/validation/*`), wallet-env clean. No marketplace/checkout/security/Stream/payroll/swap/Telegram/x402 changes beyond the concrete Build 2 dependency (hire + complete).
- **Human-Worker Hiring Loop batch, 2026-09-01 (two job modes made explicit — Direct Hire vs Open Procurement)**: The /jobs page is now two coherent hiring modes instead of one wizard forcing the merchant to name a provider. **Mode 1 Direct Hire** (unchanged wizard, retitled): provider fixed on-chain at createJob; provider signs setBudget + submit; best for self-tests with your own agent. **Mode 2 Post a Job** (new tab): merchant posts a procurement posting (`POST /api/procurement`, needs `clientAgentId` of one of their ACTIVE agents as payer), workers discover it on Telegram (`/jobs` lists procurement postings ONLY — legacy OPEN Erc8183Jobs dropped from the listing), `/apply <postingId> <pitch>` writes a `ProcurementApplication`; merchant views ranked applicants → Select → Hire; worker then signs from Telegram. **Code changes**: (1) `hire/route.ts` now accepts human providers — when `selectedProviderSCA` has no `AgentRegistry` row, resolves `ConsumerAccount` by wallet (must have `circleWalletId`), skips `ACTIVE_AGENT_PROVISIONED`, applies neutral-50 trust baseline (fail-closed if treasury `minTrustScore > 50`), keeps treasury/spend-limit on the client agent; (2) `accept/route.ts` (`/api/jobs/[jobId]/accept`) now accepts human providers — resolves the signing wallet from `ConsumerAccount.circleWalletId` (never the body), skips `evaluateProviderAcceptance` (agent-only policy) for humans; wallet id always DB-derived; (3) new Telegram `/accept <jobId> [amount]` command (`handleAccept` in `botHandlers.ts`, dispatched in webhook, in `/help`) — calls the real accept route internally with a `consumer_token` cookie (mirrors `/deliver`); amount optional, route prefers the hire's intended budget; (4) hire now best-effort notifies the hired worker on Telegram ("You've been hired... send /accept N..."), completing the previously-unwired "You'll be notified if you're selected" promise; (5) legacy direct-hire `/apply` BLOCKED + steered: bot `handleApply` on a numeric Erc8183Job replies "direct-hire... provider assigned at creation, can't be applied for", `handleListJobs` no longer lists legacy jobs, and `POST /api/jobs/[jobId]/apply` refuses real legacy jobs with `409 {code:"DIRECT_HIRE_NO_APPLICATIONS"}` (unknown job still 404, uncontrolled caller still 403 — `submitApplication`/`getRankedApplicants` remain only for historical rows; applicants GET route untouched); (6) `GET /api/agents/discover?mine=1` scopes to the authenticated merchant's own agents (401 without session — no cross-tenant leak); (7) `/jobs` page: tabs renamed `🎯 Direct Hire` / `📢 Post a Job` / `🔧 Manage`, board tab explains the two modes, provider-field helper text no longer claims `/balance` shows an address (it never did — only `/withdraw` surfaces addresses), Step 2 Set Budget card explains "signed by the PROVIDER's wallet", and the Post a Job tab has a posting form (pick your agent, budgetMax, description) + "My Postings" list with ranked-applicants viewer and Select & Hire buttons; after hire the UI tells the worker to run `/accept <jobId>` on Telegram and fund/complete via the Manage tab. **Bug found + fixed by e2e**: accept returned 500 for human providers — `providerAgent.id` in the success payload crashed when `providerAgent` was null; fixed to `providerAgent?.id ?? null` and Circle error messages in accept's tx catch now extract `response.data.message` instead of the empty `.message`. **E2E `scripts/procurement-human-e2e.ts` 21/21 (real testnet funds)**: merchant login → posting → worker (fresh Circle wallet via `provisionWalletForTelegramUser`, NO AgentRegistry row) applies → ranked → select → hire (human) → worker `/accept` (consumer cookie; on-chain budget verified via `getJob` == 0.02) → merchant funds → worker delivers → complete → worker paid >0.008 USDC after fee, job COMPLETED, posting HIRED. Script self-funds the client agent (0.5 USDC native top-up from x402 pool + ledger `REVENUE` CREDIT dedupe row, both cleaned up). **Handler tests `scripts/telegram-accept-tests.ts` 20/20**: /accept gating (no session / bad jobId / bad amount / no Circle wallet), /apply steer + zero rows, /jobs procurement-only, static proofs for human branches + notification + refusal code + webhook dispatch. **Test updates**: `telegram-hardening-tests.mjs` §3 now asserts the steer (zero `JobApplication` rows, was duplicate-rejection); `http-auth-tests.mjs` jobs-create case asserts 401 (the route is `withApiKeyOrAnySession`-wrapped, so anonymous hits auth before the control check) and uses distinct client/provider addresses (self-hire guard 400s identical ones); `scripts/build5-route-tests.mjs` 36/36 confirms the AGENT-provider hire/accept/fund path is unchanged (1 pre-existing BLOCKED: platform agent not in registry). **Regression**: `tsc --noEmit` clean, `next build` pass, telegram-hardening 27/28 (the 1 fail is the pre-existing env flake — `CIRCLE_WALLET_SET_ID` set so /start took the live-mint path), telegram-completion 18/18, http-auth-tests 17/17, telegram-accept 20/20, build5-route 36/36, procurement-human-e2e 21/21.
- **Semantic Job IDs + Admin Moderation follow-up, 2026-09-01**: (1) **`job<N>` semantic ids for Telegram apply** — `ProcurementPosting.seq` autoincrement added (migration `20260901153929_add_procurement_seq_and_job_removal`; also adds `Erc8183Job.removedAt/removedReason`). `/jobs` now lists `/apply job<N> "<pitch>"`, and `handleApply` resolves `job<N>` (or the raw cuid) via `resolvePostingId` in `botHandlers.ts`. (2) **Quoted pitches + usage** — `/apply` alone replies "Input the job id and your pitch, e.g. /apply job112 "I can build this in 2 days". Put your pitch in quotes."; `handleApply` strips one layer of surrounding quotes (`stripQuotes`) before storing; `/help` + jobs-page Post-a-Job copy updated. (3) **Admin job moderation** — `GET /api/admin/jobs` (lists ERC-8183 jobs + procurement postings with `humanId`), `POST /api/admin/jobs/[jobId]/remove` (DB: status REJECTED + removedAt/removedReason + cancels linked OPEN posting; on-chain best-effort `reject` ONLY when on-chain status is still OPEN, signed with the evaluator's Circle wallet; FUNDED/SUBMITTED jobs left untouched — escrow is one-way, no admin clawback), `POST /api/admin/postings/[id]/remove` (accepts `job<N>` or cuid, cancels posting + deletes applications), and `/admin/jobs` page (list + Remove buttons + reason prompt) linked from `/admin`. (4) **`scripts/build5-route-tests.mjs` REMOVED in this commit (restored later from `580e143^` — see Restore Build 5 Route Regression note below)** + 13 leftover "Build5 route test work" procurement postings and 9 jobs cleaned from the DB. **Tests**: `telegram-accept-tests.ts` extended to 37/37 (job<N> resolve, quote stripping, /jobs semantic listing, admin static proofs). **Regression**: tsc clean, build pass, telegram-hardening 27/28 (same pre-existing env flake), http-auth 17/17, procurement-human-e2e 21/21.
- **Restore Build 5 Route Regression, 2026-09-01**: `scripts/build5-route-tests.mjs` was deleted in `580e143`; restored byte-exact from `580e143^` (identical to `2f3ac04`) after independent verification showed it is still valid against the current repository (agent-provider hire/accept/fund, F6 brain static proofs, F8/F12 auth). Zero compatibility edits were needed — the only interface diffs since deletion are backward-compatible human-provider additions to `accept/route.ts`/`hire/route.ts` (agent response shape unchanged: `jobId`/`txHash`/`replayed`/`budget`/`provider`). Re-run result: **36/36 passed, 0 failed, 1 BLOCKED** (pre-existing: platform agent `AGENT_OWNER_WALLET_ADDRESS` not in registry — the same known BLOCKED documented in the Human-Worker batch). The Build 5 route regression coverage is permanent again. Also added a **Get Test Tokens** link (Circle USDC faucet, https://faucet.circle.com/) to the shared `DashboardSidebar` FAUCET nav group.
- **Job Flow Fixes follow-up, 2026-09-01 (treasury funding + wording + units)**: (1) **`POST /api/agents/[id]/treasury/credit`** closes the "Treasury policy blocked: insufficient available balance 0" gap — a fresh agent has ledger treasury 0 and there was NO merchant-facing way to fund it (only the e2e did, by hand). Endpoint moves REAL USDC: source = the authenticated merchant's own Circle wallet, destination = the agent's Circle wallet (server-derived from DB, never from the request body — body carries only `amountUSDC`; dest must resolve to `agent.scaAddress`, fail-closed). Preflight merchant balance → `transferUsdc` → measure the agent's actual received delta (balanceOf before/after — Arc charges a per-transfer fee) → record an **`ADJUSTMENT` CREDIT** ledger entry deduped by txHash (deliberately NOT `REVENUE`, so trust scores are not inflated by top-ups). Jobs page Post-a-Job tab now shows the selected agent's treasury and a "Fund agent treasury (USDC)" button; the Select & Hire error path adds a "fund your hiring agent" hint when the treasury gate blocks. (2) **Budget units bug**: the posting form sent the raw `budgetMax` string — a bare integer "2" was parsed by `toUnits` as 2 micro-USDC (0.000002 USDC), which is exactly what the user's job27 posting showed. Form now normalizes to a 6-dec string ("2" → "2.000000" = 2 USDC) and previews "Will be posted as 2.00 USDC" beside the field. (3) **Bot wording**: `/apply` success/already-applied/not-open replies use the human `job<N>` id instead of the cuid (`Application submitted for job job27…`), and `/jobs` drops the "(procurement)" jargon. (4) **Applicant score transparency**: applicants list shows the breakdown the scorer already returns (`trust 20/40 · pitch 5/20 · price 12/25 · recency 0`) plus "new workers start neutral (20/40)" — the bare "score 37" was an unexplained grade (it is NOT a person's rating: automated `getRankedProcurementApplicants` = trust/reputation 20 + price 12 (no bid) + completeness 5 (short pitch) + recency 0). Tests: `telegram-accept-tests.ts` 39/39 (job<N> reply not cuid, no "(procurement)", treasury endpoint auth/derivation static proofs via new credit route file); `procurement-human-e2e.ts` **23/23** now funds the client agent through the REAL credit endpoint (real USDC merchant→agent, was 21/21 with a manual ledger row). Regression: tsc clean (`.next/types` errors are pre-existing stale artifacts from deleted escrow routes in unrelated WIP), build pass, http-auth 17/17, telegram-hardening 27/28 (pre-existing CIRCLE_WALLET_SET_ID env flake), telegram-completion 18/18. No new migration — only `ADJUSTMENT` ledger rows, no schema change.
- **Escrow Beneficiary batch, 2026-09-01 (beneficiary is a first-class party — merchant/consumer/agent/external)**: (1) `src/lib/escrow/resolveBeneficiary.ts` classifies `beneficiarySCA` at creation into `beneficiaryKind` = merchant (a `Merchant.walletAddress`) | consumer (`ConsumerAccount.walletAddress`) | agent (`AgentRegistry.scaAddress`) | external (anything else — first-class case, not an error); stored on the `Escrow` row (migration `20260901170000_escrow_beneficiary_fields` adds `beneficiaryKind` + `beneficiaryNotifiedAt`), set by BOTH `/api/escrow/create` and `/api/merchant/escrow-link`. (2) `src/lib/escrow/notifyBeneficiary.ts` — best-effort, idempotent (`beneficiaryNotifiedAt` guard) notification at creation AND at link-funding when the escrow becomes ACTIVE: merchant → central `notify()` dispatcher; consumer → Telegram DM when `telegramUserId` exists; agent → its OWNER merchant; external → no channel (depositor holds the shareable link). NEVER fails the create/fund flow. (3) Creation hardening: `isAddress(beneficiarySCA)` 400 + self-escrow blocked ("depositor and beneficiary must be different addresses") in create route. (4) Incoming list: `GET /api/escrow/list?role=beneficiary` — scoped by `getCallerControlledAddresses` (same trust set as release: own wallet, buyer EOA, owned agents' SCAs + payment EOAs), returns ACTIVE/total metrics + per-row `confirmUrl`; the depositor does NOT see it in their own incoming view. (5) **External-EOA confirm path**: `POST /api/escrow/[reference]/beneficiary-confirm` (public, no auth, never moves funds — no Circle SDK) PROVES the beneficiary's own on-chain `confirmDelivery` before mirroring: tx sender == beneficiarySCA, tx.to == escrow contract, calldata selector == `escrowSelectors.confirmDelivery`, decoded bytes32 arg == `contractEscrowId` (== keccak256(reference)), receipt status success, THEN re-reads AUTHORITATIVE `getEscrow` at the receipt block (latest-block fallback) and mirrors — one-sided confirmed stays ACTIVE with that flag, dual-confirm auto-release becomes RELEASED (ledger unlock + webhook same as release route). Same authoritative-read invariant applied to `/api/escrow/release` (fails closed 500 if the state read is unavailable — never flips from body+receipt alone). `src/app/escrow-confirm/[reference]/page.tsx` is the public page where the beneficiary signs `confirmDelivery` from their own wagmi-connected wallet (no custodial path). (6) UI: Escrow page has My Escrows / 📥 Incoming tabs (Incoming actions use `beneficiarySCA` as caller), DashboardSidebar PAYROLL group (Batch Payroll / Scheduled Payroll / Payroll Chat) + a green incoming-escrow badge on Escrow (polls `role=beneficiary` metrics), and checkout/escrow-pay now show a hard Wrong-network gate (Pay blocked, no "Pay anyway") that switches via the ACTIVE connector's provider (WalletConnect-aware `ensureArcNetwork`). (7) E2E `scripts/escrow-beneficiary-e2e.mjs` **21/21** (real testnet funds): creation hardening, merchant-B incoming → one-sided confirm → auto-release with beneficiary balance delta, external fresh-EOA (native-funded for gas) confirm via the public route, negative cases (wrong-sender 400, wrong-escrowId 400 + "DIFFERENT escrow id", fabricated hash 400, non-beneficiary caller 403 — row untouched each time), notification idempotency (funded links notified once; re-invoke reports already-notified). `scripts/escrow-link-tests.mjs` extended to **42/42** with Phase 1-5 static proofs. NOTE: `_prisma_migrations` on BOTH Neon (dev) and the `.env` Render legacy DB hold a SECOND failed record for `20260901170000_escrow_beneficiary_fields` (finished_at NULL, "relation escrows does not exist" from an earlier `migrate dev` attempt) — `prisma migrate deploy` reports "no pending" on both; the Render DB does NOT actually have the columns while its successful record claims applied — **verify/repair before any prod deploy that reads `.env`**. Regression: tsc clean, next build 121/121, escrow-beneficiary-e2e 21/21, escrow-link-tests 42/42, wallet-ux-tests 46/46, payroll-assistant-tests 26/26.
- **Agent identifier centralization + deploy cost control + validation notify batch, 2026-09-03 (resolveAgentRef Track 2, direct-hire hardening, deploy guard, brain/escrow/procurement fixes)**: (1) `src/lib/agents/resolveAgentRef.ts` (Track 2 - central Agent identifier resolver): accepts all three non-interchangeable `AgentRegistry` identifiers - `id` (small DB int, canonical), `tokenId` (ERC-8004 digit-string), `scaAddress` (0x wallet) - via an explicit type or `auto`. Semantics: `id` = strict small-integer `findUnique`, never re-interpreted as tokenId; `tokenId` = exact digit-string match, never treated as a DB id; `scaAddress` = case-insensitive; `auto` = deterministic disambiguation only (0x-shape routes scaAddress; same digits matching DIFFERENT id+tokenId records -> `ambiguous: true`, refused, no silent guessing). Returns `{agent, matchedBy, ambiguous}` with `agent: null` (never throws -> callers emit clean 404 instead of 500) for unknown/malformed refs; `auto` is reserved for genuinely user-facing paste fields, internal services pass an explicit type. Wired into `GET /api/agents/[id]/ledger`, `GET /api/agents/[id]/track-record` and the agents marketplace filters (`agents/page.tsx`). Tests `scripts/agent-resolver-tests.ts` 19/19 (real DB fixtures + cleanup). (2) **Direct Hire create hardening (`POST /api/jobs` action=create)**: provider resolved from DB BEFORE any on-chain/DB-write/chain-read work - `AgentRegistry.scaAddress` lookup (case-insensitive) -> unregistered provider 400 ("providerSCA not registered - no on-chain tx attempted"); `ConsumerAccount.walletAddress` fallback keeps human hires working; best-effort Telegram notify to provider AFTER the DB write and BEFORE success return (try/catch, failure logged with tag, never fails create; agent provider without telegram -> skip-with-log - no new channel); `prisma.job.create` failure now observable (real error logged server-side, no silent bare catch - success response preserved after DB write). Tests `scripts/direct-hire-create-tests.ts` 45/0 (real Prisma fixtures + static source-ordering proofs + a narrow `global.fetch` stub for the Telegram transport ONLY - chain-touching success cases proven statically, never faked txHash/jobId). (3) **Zero8 compat (`f815b03`)**: insufficient-funds matcher extended in escrow create hint + CheckoutWidget error mapping to also match `OutOfFunds` / `out of funds` / `outoffunds` / `gas required exceeds allowance` (case-insensitive) -> same "fund the depositor SCA at faucet.circle.com" / "Insufficient funds for this transaction." friendly paths. (4) **Agent deploy cost control**: (a) `src/lib/agent-deploy-guard.ts` - dependency-free (no next/server, prisma, or Redis - importable from edge runtimes + plain-node tests) merchant-scoped gate: values/patterns mirror `RATE_LIMITS.agent` (10/min fixed-window) rather than import it (would drag next/server in); idempotency-key dedupe (same convention as agentPay.ts - trim + 120-char cap, merchant-scoped, 24h TTL, replay -> **409** "replay of in-progress request" - checked first so replays never burn rate budget); keyless **5s burst throttle** for accidental double-submits (double-click/retry storm); NO one-agent-per-merchant cap - distinct idempotency keys always allow legitimate multi-agent provisioning; `releaseAgentDeployClaim()` frees the key on terminal pre-transaction failure so same-key retries work (guard holds NO wallet references, never touches Circle wallets, cannot orphan them); bounded in-memory stores. (b) wired into `POST /api/agent/deploy` AFTER `withMerchantAuth` resolves the authoritative merchant, BEFORE Circle wallet provisioning and the on-chain register (unauth/empty merchant refused - no default-payer fallback). (c) deploy identity fix (`aa13459`): never synthesize a fallback ERC-8004 identity - missing Transfer log -> truthful `PENDING_IDENTITY_CONFIRMATION` response (carries tx hash + inspection info, `retryable: true`), `ACTIVE_AGENT_PROVISIONED` persisted ONLY after real tokenId proven from chain logs. Tests `tests/deploy-identity.test.mjs` 7/7 + `src/lib/agent-deploy-guard.test.ts` 8/8 (incl. wiring-order invariant: guard fires before Circle provisioning + on-chain register).
(5) **Validation validator notification (`3472bcb`)**: `src/lib/notifyValidator.ts` - after on-chain validation request SUCCESS (agent route after `recordValidationRequest` / job path after `waitForTx`, both after the exact 403 auth guards, position verified statically - idempotent replay never re-notifies), maps the AUTHORITATIVE validator wallet (the exact `validator` arg - `validatorSCA` body field / job route `policy.validatorSCA`; echoed verbatim, never regenerated or fabricated) via the existing read-only `resolveBeneficiary()` classifier to EXISTING channels only: merchant -> central `notify()` event `validation.requested`; consumer -> Telegram DM when `telegramUserId` exists (no-telegram still notified, no throw); agent -> its OWNER merchant; external -> link-shared respond hint (requestHash + endpoint already returned in the request response). Payload carries no private info (validator's own address, public agent tokenId/name, request tag/hashes, explorer URL `https://testnet.arcscan.app/tx/...`, respond instructions; no emails/keys/balances); NO payer/wallet resolution and NO default-payer fallback (caller passes already-authorized on-chain values straight through). Failure semantics: NEVER throws - errors caught, logged (`[notifyValidator] ... failed`), reported `{notified:false}`. Docs note: receiver pending-inbox still a GAP (no `ValidationRequest` table - schema+UI change deliberately left out of this subtask; until then the validator discovers work via this notification and responds `POST /api/agent/validation { action:"respond", ... }`). Tests `scripts/validation-notify-tests.ts` 15/15 (mocked transports, node:test, no DB/chain/network). (6) **Escrow beneficiary metrics crash-safe (`547fac4`)**: `/escrow` Incoming/metrics rendering hardened - `normalizeMetrics` returns the full `EscrowMetrics` contract (undefined/null/`{}` -> all-zero, no crash; partial shapes -> rest zeroed; string numerics coerced (Decimal-over-JSON safe); garbage numerics -> 0, never NaN; no unguarded `selected.amount.toFixed`; metric cards render via `fmtAmount` (never throws)). Tests `scripts/escrow-beneficiary-metrics-tests.mjs` 33 pass/0 fail/1 skip (no server for the live beneficiary-auth 401 check - DB + static + transpiled-runtime guard cases all pass). (7) **Procurement BigInt serialization (`af97993`)**: `GET /api/procurement` serializes `resultingJobId` BigInt -> string when populated (was `TypeError: Do not know how to serialize a BigInt` -> UI `JSON.parse: unexpected end of data` on HIRED postings); `null` stays null; OPEN/SELECTED/HIRED response shapes stay compatible. Tests `scripts/procurement-bigint-e2e.ts` 15/15 (real route export + Prisma fixtures). (8) **Agent Brain dead-EOA removal + truthful Groq 429 (`09126a4`)**: agent-brain page dead client-EOA wallet field removed (pay body sends ONLY message + sessionId; server resolves the payer - x402 requires EOA, Circle SCAs cannot be x402 payers, explainer kept but nothing client-controlled is trusted); Groq upstream failures truthful: 429 -> HTTP 429 `success:false` + rate-limit code + `Retry-After` honored (body `retryAfterMs` fallback); 5xx/network -> 503 retryable, malformed -> 502, missing message -> 400, missing `GROQ_API_KEY` -> 500; `BrainUpstreamError` caught + returned with its mapped status. Tests `scripts/brain-tests.ts` 20 checks/0 failures (offline, exercises the real route exports, no Groq key/server needed). Regression re-run on resume: `tsc --noEmit` clean, `next build` pass (compiled 54s, static 122/122), deploy-identity 7/7, deploy-guard 8/8, validation-notify 15/15, direct-hire 45/0, resolver 19/19 (after removing 2 stale `ResolverA/B` fixture rows left by the original session run - fixed tokenIds `99886202/3`; P2002 on re-run is the fixture-collision symptom, not a code regression), procurement-bigint 15/15, escrow-metrics 33/0/1skip, brain 20/0. No schema/migration changes in this batch.
- **Direct Hire create - serviceability gates, 2026-09-04 (subtask B follow-on to `367c6a1`)**: `POST /api/jobs` action=create now rejects registered-but-UNSERVICEABLE providers with a clean 400 BEFORE any chain interaction, matching the "real supported path" of the validated procurement hire route (AGENTS.md invariant: never mint a job the lifecycle cannot move). (1) `AgentRegistry.scaAddress` lookup broadened to select `status` + `circleWalletId`; a registered agent that is not `ACTIVE_AGENT_PROVISIONED` -> 400 ("providerSCA is a registered agent (registry id N) whose status is 'X', not ACTIVE_AGENT_PROVISIONED - only fully provisioned agents can service a Direct Hire job" - same status gate as `agents/[id]/hire`); a provisioned agent with no Circle wallet -> 400 (Direct Hire requires the provider's Circle wallet to sign setBudget and submit on-chain). (2) `ConsumerAccount` human-provider fallback also selects `circleWalletId`; a known wallet with no Circle wallet -> 400 (same real supported path). Rationale documented inline: the Direct Hire lifecycle is executed BY the provider (setBudget + submit are signed from the provider's Circle wallet), so an un-provisioned agent or wallet-less provider can never progress the job past Open - reject at create instead of minting a dead job. All three gates sit between the provider lookups and `verifyCallerControlsAddress`/`publicClient.getBlock()`/`createContractExecutionTransaction`, i.e. before the first chain read or on-chain createJob tx; NO default-payer fallback introduced. Tests `scripts/direct-hire-create-tests.ts` **78/0** (was 45/0): new `[a2]` static proofs (gate selects status+circleWalletId; message text present; gate ordering after lookups, before caller-control/getBlock/createJob; zero chain calls above the gates; 400-returns count 8), new `[f]` real-DB fixture checks (pending/no-wallet agent + no-wallet human all resolve-but-would-400), and new `[f-live]` block that invokes the REAL exported `POST` handler against the REAL DB with `global.fetch` replaced by a throwing record spy - every rejected provider (unregistered/pending-agent/no-wallet-agent/no-wallet-human) returns a clean 400 with ZERO chain/network fetch calls, and serviceable agent/human providers pass the gates and fall through to the 403 caller-control check (still zero chain calls). One static assertion was adjusted on resume: the agent-status error string is emitted across two adjacent template-literal lines in the route (full single-line phrase already asserted at runtime via EXPECTED_AGENT_STATUS_400_MID in `[f-live]`), so the static check now asserts both contiguous source fragments. Regression on resume: `tsc --noEmit` clean (exit 0), `next build` pass, direct-hire 78/0; `scripts/http-auth-tests.mjs` unaffected (its `/api/jobs` create case is anonymous -> 401 at the `withApiKeyOrAnySession` wrapper, before the handler's gates; needs a live server so not rerun here); `nanopayment-stream-e2e.mjs` uses `/api/jobs/nanopay/open`, not the create path. No schema/migration changes (gate reads existing `AgentRegistry.status`/`circleWalletId` and `ConsumerAccount.circleWalletId` columns only).
- **ERC-8004 tx-hash recovery endpoint, 2026-09-04 (subtask C - smallest safe recovery, follow-on to the fail-closed orphaned-identity handling in deploy/route.ts)**: `POST /api/agent/deploy/recover` closes the one remaining window - "register() succeeded but the server lost the response / token-extraction failed" - WITHOUT weakening the fail-closed design (still no fake `ERC8004-FALLBACK` tokenId, no Prisma PENDING state, no schema change, no fabricated ACTIVE row). Flow: merchant auth (`withMerchantAuth`) -> require `txHash` (0x-64-hex; NOT a body tokenId - a body-supplied tokenId is never accepted) -> fetch the authoritative Arc receipt via viem `getTransactionReceipt` (3 retries for flaky testnet RPC) -> derive the REAL identity (holder + tokenId) from the receipt logs via a new pure helper `src/lib/agents/agentRegisterRecovery.ts` (`extractIdentityMintFromLogs`: emits from exactly `IDENTITY_REGISTRY`, must be an ERC-721 MINT = Transfer with from=zero address, exactly 4 topics, positive tokenId; later transfer / different contract / malformed log -> null, never guessed) -> **ownership gate**: the minted holder must be in the authenticating merchant's `getCallerControlledAddresses` set (the repo's single ownership gate; never trusts `ownerAddress` alone) -> 403 if not controlled (blocks attaching another merchant's registration) -> idempotent resolve (existing token/SCA row returned; another merchant's row -> 409; P2002 race re-read + return) -> persist via the SAME successful-deployment path as deploy/route.ts §7 (`agentRegistry.create` ACTIVE + `merchantId`). NEVER creates Circle wallets and NEVER submits a second registration (no `createWalletSet`/`createWallets`/`createContractExecutionTransaction` in the route). Tests `scripts/register-recovery-tests.ts` **26/0** (pure extractor unit tests + static security proofs for all 15 requirements); `tests/deploy-identity.test.mjs` **7/7** unchanged. Known limitation documented in the route: the recovered SCA came from the deploy route's temporary wallet set (not persisted to Merchant/rows), so (a) the recovered agent's `circleWalletId` is written `null` and may need re-linking, and (b) a brand-new SCA that no other controlled-address relationship can prove is refused by the ownership gate even for its rightful owner - fail-closed and safe, but a portion of the true orphaned-SCA case may not be recoverable through this endpoint alone (see unresolved limitations; revisit with Track 3).
- **Agent resolver int4-overflow hardening, 2026-09-04 (subtask D - minimal, Track 2)**: `src/lib/agents/resolveAgentRef.ts` `findById` (explicit `id` mode) now bounds the candidate to the signed int4 range (`Number.isSafeInteger(id) && id > 0 && id <= INT4_MAX` = 2,147,483,647) so an int4-overflowing value (e.g. `3000000000`, a 30-digit tokenId, uint32-max+1) returns the resolver's clean not-found/null instead of being handed to Prisma as a where-clause id (which would have thrown). Never-throws contract preserved; the `auto` path already bounds ids to `MAX_PLAUSIBLE_REGISTRY_ID` (1_000_000) so it never reaches Prisma out of range. Sibling-route analysis (documented only, NOT a broad refactor): ledger + track-record already resolve via `resolveAgentRef` (id/tokenId/SCA). Genuinely user-facing reference routes that COULD accept tokenId/SCA for correctness - `[id]/card` (public agent card; currently id-only), `[id]/acceptance-policy` (public read), `[id]/hire` (validates agent status; accepting tokenId would improve developer ergonomics) - were inspected but NOT migrated: they are id-canonical, the AGENTS.md invariant keeps them strict, and the instruction forbids a broad refactor. `policy`/`wallet`/`pay`/`treasury`/`treasury/credit` are strict canonical-ID + caller-control endpoints and MUST stay id-only (accepting tokenId/SCA there would be an auth risk, not an improvement). Tests `scripts/agent-resolver-tests.ts` **24/0** (was 19/0): new explicit-id overflow cases (`3000000000` number+string, 30-digit MAX_SAFE overflow, uint32-max+1) all `agent === null`, plus a never-throws proof. No authorization weakening: none of these routes changed behavior. No schema change.
