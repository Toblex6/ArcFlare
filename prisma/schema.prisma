generator client {
  provider = "prisma-client-js"
  // Remove or comment out any 'output = ...' line here
}

datasource db {
  provider = "sqlite" // or postgresql depending on your database setup
  url      = env("DATABASE_URL")
}